name: legal_import
description: >
  Pipeline d'import du corpus juridique burkinabè et OHADA dans la table
  Supabase `legal_documents` / `legal_articles` de JurisIA.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  args: ^2.5.0
  http: ^1.2.0
  html: ^0.15.4

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
